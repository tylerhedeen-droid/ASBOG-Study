# USACE ASBOG Study Tool — v5.7.1 Answer Balance Fix

Critical answer-position QA fix:
- Rebalanced correct-answer positions across A/B/C/D within every domain × difficulty practice group.
- Rebalanced each dedicated FG exam form by domain as well.
- Preserved every Bank ID and every question's correct answer content.
- Only answer-choice order changed.
- Existing Supabase accounts, progress schema, cloud sync, and local-storage key are unchanged.

Replace repository-root `index.html` and commit.
