# USACE ASBOG Study Tool — v5.3 Concept-Complete Feedback

This update improves immediate feedback so every explanation is sufficiently complete to understand the concept, regardless of difficulty label.

Also adds a 1-question session option.

Existing Supabase accounts and progress are preserved. The storage key, Supabase table, user IDs, question IDs, and progress schema are unchanged.

Replace the repository-root `index.html` and commit. GitHub Pages will redeploy automatically.
